# 디자인

## 기준 문서

- **상태:** 활성
- **작성일:** 2026-09-15
- **제품 화면:** Android 앱(XML + DataBinding), iOS 앱(SwiftUI), 웹(Next.js)
- **검토 근거:** `android/app/src/main/res/layout/activity_main.xml`, `activity_setup.xml`, `activity_settings.xml`, `activity_timetable.xml`, `activity_schedule.xml`, `activity_meal.xml`, `activity_timer.xml`, `item_timetable_entry.xml`, `android/app/src/main/res/values/{colors,dimens,themes}.xml`, `docs/android-ui-refresh-mockup.md`, `docs/project_specification.md`, `web/components/app-page.tsx`, `web/components/dashboard-card.tsx`, `web/components/data-state.tsx`, iOS 앱 구조. Storybook 또는 승인된 스크린샷 기준선은 발견되지 않았습니다. 이번 개선을 위해 2026-09-15에 QA 단말(SM-A346N, 1080×2340, 시스템 글자 크기 1.15배)에서 촬영한 화면을 검토했습니다. 조정 후 홈 화면 캡처에서는 밝은 페이지 배경 위 상태 표시줄의 어두운 콘텐츠가 읽기 쉬우며, 기존 홈 히어로 영역의 시각적 비중이 컸음을 확인했습니다.
- **근거와 추론의 구분:** Android XML의 크기와 계층은 관찰된 사실입니다. 아래 우선순위는 글자가 다소 크게 느껴진다는 사용자 의견과 해당 관찰을 바탕으로 한 디자인 권고입니다.

## 브랜드

- **성격:** 차분하고 신뢰할 수 있으며, 학교생활에 실용적이고 친근하지만 유치하지 않음.
- **신뢰 요소:** 명확한 날짜·학급 맥락, 출처·오류 상태 표시, 예측 가능한 뒤로 가기, 명시적인 개인정보 선택권.
- **피할 것:** 과도하게 큰 마케팅형 히어로 글자, 밀도 높은 대시보드 장식, 대비가 낮은 파스텔 글자, 숨겨진 네트워크 실패, 같은 기능에서 Android·웹·iOS가 다르게 보이는 경험.

## 제품 목표

- 다음 수업, 오늘 급식, 가까운 학사 일정을 3초 안에 훑어볼 수 있게 한다.
- 휴대폰에서의 시각 피로를 줄인다. 경쟁하는 제목을 줄이고, 문구를 짧게 하며, 여백 리듬을 일관되게 유지한다.
- 초기 설정, 날짜 탐색, 타이머 제어, 누락 데이터 복구를 설명 없이도 이해할 수 있게 한다.
- **범위 제외:** 데이터 모델 재설계, Android Compose 전환, 시각적 새로움만을 위한 신규 기능 추가.
- **성공 신호:** 사용성 점검에서 스크롤 없이 다음 행동을 찾을 수 있음; 글자 크기 1.3배에서 한글이 잘리지 않음; 초기 설정 완료 및 시간표/날짜 탐색이 되돌아가기 없이 완료됨; 접근성 점검에서 심각한 대비·터치 영역 문제가 없음.

## 사용자와 핵심 과업

- **학생(주 사용자):** 수업 사이 짧은 시간에 한 손으로 확인하며, 지금 다음에 무엇을 언제 어디서 해야 하는지 즉시 알고 싶어 함.
- **학부모/교사(보조 사용자):** 급식 또는 학사 일정을 가끔 확인하며, 장식보다 날짜와 학교 정보의 명확성을 중시함.
- **첫 사용자:** 학교와 학년/반을 한 번 설정한 뒤, 앱 사용을 막지 않는 선택형 텔레메트리 동의를 부담 없이 이해해야 함.

## 정보 구조

- **진입:** 스플래시 → 초기 설정(학교/반 또는 동의 절차가 미완료된 경우) → 홈.
- **홈:** 학교/학급 및 날짜 맥락 → 오늘 요약 → 시간표, 급식, 일정, 공지, 타이머 카드.
- **상세 화면:** 시간표(일간), 급식(일간/주간), 학사 일정(월간), 타이머, 설정.
- **설정:** 학생 프로필, 타이머 환경설정, 텔레메트리 동의. 뒤로 가면 이전 화면으로 돌아감.
- **콘텐츠 위계:** 페이지 제목 > 현재 날짜/학급 요약 > 핵심 상태/개수 > 항목 제목 > 보조 설명 > 복구 행동.

## 디자인 원칙

1. **읽기 전에 훑기:** 영역마다 대표 제목 하나와 주요 행동 하나를 둔다.
2. **정보 밀도는 의도적으로:** 터치 영역을 줄이기 전에 글자 크기를 조정하고, 편안한 행 높이는 유지한다.
3. **하나의 화면, 하나의 리듬:** 모든 화면에서 페이지 여백, 카드 모서리, 구분선, 행동 표현 방식을 통일한다.
4. **상태도 콘텐츠:** 로딩, 비어 있음, 오래된 캐시, 오류, 설정 완료 상태를 서로 구분하고 행동할 수 있게 표현한다.
5. **점진적 공개:** 요약을 먼저 보여주고, 긴 설명·개인정보 상세·보조 제어는 명확한 진입점 뒤에 둔다.
6. **예상하지 못한 확정 방지:** 삭제 또는 개인정보에 영향을 주는 행동은 명시적으로 확인하며, 기본값은 보수적으로 둔다.

## 시각 언어

- **색상:** Android 토큰 `bg_page`, `surface_card`, `brand_navy`, `brand_blue`와 의미별 연한 배경/글자 조합을 유지합니다. 본문은 접근 가능한 대비를 확보하고, 상태를 색상만으로 전달하지 않습니다.
- **서체:** 화면마다 임의의 크기를 늘리기보다 조밀한 서체 단계를 사용합니다. Android의 `sp`는 사용자 글자 크기 설정을 반영하며, Android 14 이상은 최대 200%까지 글자 크기 조절을 지원하므로 모든 학생이 1.0배를 쓴다고 가정하고 기준 크기를 낮추면 안 됩니다. [Android 단위 가이드](https://developer.android.com/design/ui/mobile/guides/layout-and-content/grids-and-units)는 `sp` 사용을 권장하며, [Android 14 가이드](https://developer.android.com/about/versions/14/features)는 최대 글자 크기 테스트를 요구합니다.
- **승인된 조밀형 기준 크기(일반 본문용, 읽기 보조 모드 제외):** 페이지 제목 **22sp**, 초기 설정/홈 학교 히어로 **24sp**, 섹션 제목 **17sp**, 날짜/주간 요약 **18sp**, 본문과 버튼 라벨 **14sp**, 도움말/메타데이터 **13sp**, 작은 위젯 전용 메타데이터 **11–12sp**, 타이머 핵심 수치 **52–56sp**. 휴대폰 화면에서 본문을 14sp 미만, 앱 내 메타데이터를 13sp 미만으로 낮추지 않습니다.
- **현재 크기에서의 구체적 조정:** `text_page_title` 24→22sp, `text_hero_title` 26→24sp, `text_section_title` 18→17sp, `text_summary_title` 19→18sp, `text_body` 15→14sp로 조정합니다. `text_body_small` 14sp와 `text_metadata` 13sp는 유지합니다. 기기 점검에서 제어 요소를 침범하는 경우가 아니면 타이머는 56sp를 유지합니다. 하나의 핵심 수치는 더 큰 위계가 유리합니다.
- **학생 사용자 판단:** 읽기 가능성의 하한이 아니라 과도하게 큰 위계만 낮춥니다. 저학년 사용자나 근시/난독증 사용자는 이미 시스템 글자 크기를 1.15배 이상 사용할 수 있습니다. 테스트 단말은 **1.15배**였으며, 이로 인해 홈 히어로가 특히 커 보일 수 있지만 확장 가능한 글자를 망가뜨릴 근거는 아닙니다. WCAG 2.2 AA는 기능이나 콘텐츠 손실 없이 글자가 최대 200%까지 확대되어도 사용 가능해야 한다고 봅니다. [WCAG 2.2 SC 1.4.4](https://www.w3.org/WAI/WCAG22/Understanding/resize-text.html)
- **여백:** 페이지 안쪽 여백 20–24dp, 카드 안쪽 여백 16–20dp, 섹션 간격 16dp, 항목 간격 8–12dp. 화면별 숫자 대신 공유 `dimens` 리소스로 관리합니다.
- **형태/고도:** 페이지 카드는 반경 20–24dp, 히어로는 24–28dp, 칩은 14–18dp를 사용합니다. 모든 카드에 깊은 그림자를 적용하지 말고, 은은한 고도 또는 테두리 조합 하나를 통일해 사용합니다.
- **모션:** 로딩에서 콘텐츠로 전환될 때와 날짜 변경 시 짧은 페이드/슬라이드를 사용합니다. 모션에 필수 의미를 담지 않으며, 동작 줄이기 설정을 존중합니다.
- **이미지/아이콘:** 내용 설명이 있는 단순 선형 아이콘을 사용합니다. 핵심 텍스트 옆에 장식용 일러스트를 두지 않습니다.

## 컴포넌트

- **기존:** Android XML/DataBinding 레이아웃, Material 버튼, RecyclerView, 시간표 항목 카드, 공유 색상/크기 리소스, 웹의 `AppPage`, `DashboardCard`, `DataState`.
- **권장 공유 패턴:** `PageHeader`(제목/뒤로가기/행동), `SectionHeader`(제목 + 행동 하나), `StatusBanner`(로딩/비어 있음/오류/오래된 데이터), `DatePager`, `PrimaryButton`, `ChoiceRow`.
- **변형/상태:** 기본, 누름, 비활성, 로딩, 비어 있음, 오류, 오프라인/오래된 데이터, 선택됨/오늘, 권한/동의.
- **토큰 소유 위치:** Android `values/colors.xml` 및 `values/dimens.xml`, 웹 CSS/Tailwind 토큰, iOS 공유 SwiftUI 상수. 개별 화면에 임의의 숫자 값을 중복하지 않습니다.
- **홈 버튼 규칙(2026-09-15):** 기본 보조 행동은 48dp 이상 높이, 16dp 모서리, 14sp 굵은 글자, 네이비 글자와 은은한 네이비 테두리로 통일합니다. 어두운 히어로 안의 `시간표 보러 가기`는 흰 배경과 네이비 글자로 강조합니다. 타이머의 `시작`은 의미가 즉시 구분되도록 초록 채움과 흰 글자를 사용하며, 그 외 보조 행동과 색상을 공유하지 않습니다. 홈 설정은 텍스트·외곽선 없이 36dp 톱니 아이콘만 보이게 하고, 48dp 터치 영역을 유지한 채 히어로 우측 상단에서 학교명과 시각적으로 정렬합니다. 급식 칼로리는 연녹색 16dp 라운드 칩으로 표현합니다.
- **초기 설정·시간표 탐색(2026-09-15):** 초기 설정은 `1. 학교 선택`과 `2. 학년과 반`을 분리한 카드형 단계로 보여주고, 각 완료 행동은 동일한 48dp 파란색 버튼으로 표현합니다. 시간표의 이전/다음/오늘 및 뒤로가기는 최소 48dp 터치 영역과 14sp 글자 기준을 적용합니다. 1.3배 글자 크기에서 한 줄 힌트가 잘리지 않도록 학교 입력 예시는 짧게 유지합니다.
- **2×2 시간표 위젯(2026-09-15):** 현재 2×2 Samsung 위젯의 남는 세로 공간을 활용해 오늘/내일 수업 본문은 13sp로 표시한다. 한 수업명은 공백을 포함해 최대 6글자만 표시하고, 이후 문자는 데이터 단계에서 제거한다. 위젯 너비에서 줄임표가 추가되어 글자 폭을 더 잡아먹지 않도록 한다.

## 접근성

- 텍스트와 제어 요소는 WCAG 2.2 AA 대비를 목표로 하며, Android 접근성 가이드의 최소 48dp 터치 영역을 지킵니다.
- 아이콘만 있는 버튼에는 지역화된 콘텐츠 설명이 필요합니다. 모든 입력에는 보이는 라벨과 필드에 연결된 오류 설명이 필요합니다.
- 논리적인 포커스/탐색 순서를 유지하고, 비동기 상태 변화(`로딩`, `업데이트됨`, `오류`)를 포커스를 빼앗지 않고 알려줍니다.
- 생략한 한글 때문에 의미가 바뀌면 줄임표로 자르지 않습니다. 줄바꿈하거나 상세 보기를 제공합니다.
- 시스템 글자 크기 1.3–1.5배에서도 잘리지 않게 지원하고, TalkBack/VoiceOver로 점검합니다.
- 타이머 완료는 색상이나 모션 외에도 텍스트/상태와 선택형 햅틱·소리(설정 존중)로 알려야 합니다.

## 반응형 동작

- **Android:** 360–412dp 너비와 세로 화면의 한 손 사용을 우선 최적화합니다. 좁은 화면에서는 페이지 여백을 유동적으로 유지하며, 시간표는 세로 스크롤 목록으로 둡니다.
- **웹:** 기존의 최대 너비 대시보드를 유지하고, 카드 가독성이 확보될 때만 한 열에서 두 열로 전환합니다. 시간표를 좁은 열에 억지로 배치하지 않습니다.
- **iOS:** 기존 SwiftUI 내비게이션/탭 구조를 유지합니다. 정보 위계와 문구는 공유하되, 플랫폼별 제어 요소의 스타일을 강제로 같게 만들지는 않습니다.
- 터치 제어는 48dp 영역을 사용합니다. 웹의 호버/포커스 상태는 보조적이며, 이해를 위해 필수여서는 안 됩니다.

## 상호작용 상태

- **로딩:** 영향받는 카드 근처에 스켈레톤 또는 인라인 진행 표시를 보여줍니다. 유효한 캐시가 있으면 유지하고 오래된 데이터임을 표시합니다.
- **비어 있음:** 정말 항목이 없는지, 초기 설정이 덜 되었는지 설명하고 다음 행동을 제공합니다.
- **오류:** 쉬운 한국어로 재시도 또는 설정 중 어느 것이 필요한지 설명하고, 탐색은 계속 가능하게 합니다. NEIS 키 누락을 정상적인 부분 시간표처럼 보이게 하면 안 됩니다.
- **성공:** 짧은 개수/날짜 확인을 표시하고, 같은 내용을 토스트와 배너로 중복하지 않습니다.
- **비활성:** 행동을 할 수 없는 이유를 설명하고, 읽기 쉬운 대비를 유지합니다.
- **오프라인/느림:** 캐시 데이터에 `마지막 업데이트` 맥락과 재시도 행동을 함께 보여줍니다. 콘텐츠를 무기한 로딩 표시로 대체하지 않습니다.
- **동의:** Analytics와 진단 정보를 독립적으로 선택하게 하고, 보수적인 기본값·짧은 요약·펼쳐볼 상세 내용을 제공합니다. 핵심 사용을 막지 않는 `나중에 설정`/계속 경로를 명확히 둡니다.

## 콘텐츠 문체

- 한국어로, 짧고 안심되는 학생 친화적 표현을 사용합니다. 문단보다 한 문장을 우선합니다.
- 용어는 `시간표`, `급식`, `학사 일정`, `설정`, `다시 시도`로 일관되게 사용합니다.
- 답을 먼저 둡니다. 예: `오늘은 7개의 수업이 있어요.` 다음에 보조 설명을 둡니다.
- 비난하는 표현(`잘못 입력했습니다`)과 모호한 오류(`오류 발생`)를 피하고, 사용자가 다음에 할 수 있는 일을 말합니다.
- 버튼 라벨은 동사 중심으로 짧게 씁니다. 버튼 안에 페이지 제목을 반복하지 않습니다.

## 구현 제약

- Android는 Kotlin + XML/DataBinding + MVVM/Hilt/StateFlow를 유지합니다. Activity는 렌더링/이벤트를 담당하고, ViewModel은 상태를 소유합니다.
- 문자열은 `strings.xml`에서 지역화하고, 화면별 재정의 전 서체/여백 변경을 리소스에 중앙화합니다.
- 기존 repository/cache/telemetry 경계와 개인정보 규칙을 유지합니다. 시각 작업이 데이터 수집 의미를 바꾸면 안 됩니다.
- 단위 테스트, 린트, 일반/확대 글자 크기 단말 점검으로 검증합니다. 광범위한 레이아웃 변경 전 스크린샷/골든 테스트를 추가합니다. 현재는 스크린샷 기준선이 없습니다.
- 우선순위: (P0) 서체 크기/인셋/접근성, (P0) 홈 위계와 상태 표현, (P1) 초기 설정/설정 폼 그룹화, (P1) 시간표/날짜 탐색, (P2) 타이머 완성도와 플랫폼 간 일관성.

## 미결 질문

- [ ] **제품 책임자 — 영향 큼:** 주 대상이 360dp Android 폰인지, 더 큰 삼성 기기까지 동등 지원인지 결정이 필요합니다. 제목 줄바꿈과 카드 밀도에 영향을 줍니다.
- [ ] **제품 책임자 — 영향 큼:** 홈은 `다음 수업`을 급식보다 우선할지, 현재처럼 날짜 요약을 먼저 둘지 결정이 필요합니다.
- [ ] **제품 책임자 — 중간 영향:** 브랜드 문체는 중립적인 도구형과 약간 더 친근한 학생 도우미형 중 어느 쪽이 좋은지 결정이 필요합니다.
- [ ] **디자인/QA — 중간 영향:** 시각 회귀 기준을 위해 승인된 스크린샷 또는 기기 매트릭스가 필요합니다. 현재 저장소에는 없습니다.
- [ ] **접근성 담당 — 중간 영향:** 최소 지원 글자 크기와 TalkBack/VoiceOver 테스트 기기를 확정해야 합니다.
- [ ] **디자인/QA — P0:** 승인된 조밀형 크기 적용 후 홈, 초기 설정, 시간표, 급식, 타이머 화면을 1.0배, 1.15배, 1.3배 및 Android 최대 글자 크기에서 캡처합니다. 텍스트 잘림, 숨겨진 행동, 48dp 미만의 텍스트 전용 터치 영역이 있으면 통과시키지 않습니다.
