<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- Present only in the .qa package; adb launches it after the tester opts in. -->
        <activity
            android:name=".telemetry.QaTelemetryProbeActivity"
            android:exported="true" />
    </application>
</manifest>
